# -----------------------------------------------------------------------------
# Animal Health Radar - runtime requirements
# -----------------------------------------------------------------------------
# Pinned with >= floors that match the versions this project was developed and
# tested against (Python 3.11-3.12). Use `pip install -r requirements.txt`.
# -----------------------------------------------------------------------------

# --- core scientific stack ---------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.4,<2.0
pyarrow>=14.0

# --- schemas / config / env --------------------------------------------------
pydantic>=2.5,<3.0
PyYAML>=6.0
python-dotenv>=1.0

# --- dashboard ---------------------------------------------------------------
shiny>=1.0,<2.0
shinywidgets>=0.3
plotly>=5.18

# --- optional, gracefully degraded if absent ---------------------------------
# The pipeline and the app run WITHOUT these. Installing them upgrades the
# explanation layer (SHAP) and the dashboard styling (shinyswatch).
shap>=0.44 ; python_version < "3.13"
shinyswatch>=0.7
faicons>=0.2

# --- development / testing ---------------------------------------------------
pytest>=8.0
